<h1><%= @post.name %></h1>
  <p><%= link_to 'Back to posts', posts_path %></p>
  <h3> ID #<%= @post.id %> </h3>
  <p><%= @post.description %></p>
  <p><%= link_to 'Edit', edit_post_path(@post.id) %></p>
